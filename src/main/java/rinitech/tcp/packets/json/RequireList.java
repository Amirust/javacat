package rinitech.tcp.packets.json;

/**
 * RequireList packet sends to the server when the client wants to get the list of rooms.
 * No data is required.
 */
public class RequireList extends BasePackage {}
