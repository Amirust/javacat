plugins {
    id 'java'
}

group 'rinitech'
version '1.0-SNAPSHOT'

repositories {
    jcenter()
    mavenCentral()
    maven {
        url "https://maven.restlet.talend.com"
    }
}

dependencies {
    implementation 'org.dizitart:nitrite:3.4.4'
    implementation 'org.snf4j:snf4j-core-slf4j:1.10.0'
    implementation 'com.google.code.gson:gson:2.10.1'
    implementation 'org.restlet.jse:org.restlet:2.4.3'
    implementation 'org.restlet.jse:org.restlet.ext.json:2.4.3'
    implementation 'org.restlet.jse:org.restlet.ext.fileupload:2.4.3'
}

jar {
    manifest {
        attributes(
                'Main-Class': 'rinitech.Main',
        )
    }
}
task fatJar(type: Jar) {
    duplicatesStrategy = DuplicatesStrategy.INCLUDE
    manifest.from jar.manifest
    archiveClassifier.set('all')
    from {
        configurations.runtimeClasspath.collect { it.isDirectory() ? it : zipTree(it) }
    } {
        exclude "META-INF/*.SF"
        exclude "META-INF/*.DSA"
        exclude "META-INF/*.RSA"
    }
    with jar
}

artifacts {
    archives fatJar
}

